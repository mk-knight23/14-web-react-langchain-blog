[project]
name = "react-langchain-blog"
version = "0.1.0"
description = "A Flask + Tailwind blog project"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.0",
]

[project.optional-dependencies]
dev = [
    "debugpy",
]

[tool.setuptools]
py-modules = ["main"]